# PostgreSQL connection string, e.g. postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Use the root-level Docker DB (docker-compose.yml -> service `db`)
# Credentials: user `meghdoot`, password `meghdoot`, DB `meghdoot`, port 6545
DATABASE_URL="postgresql://meghdoot:meghdoot@localhost:6545/meghdoot"

# JWT secret used to sign sessions. Use at least 32 random characters.
JWT_SECRET="f6c1f1a892f24cfdb0f8a62a0b3a8f6a2b0d4f6f3d1c2b7a9e3f7b1c5d9a0e2"

# Optional client origin for CORS (defaults to allowing all origins during development)
# Comma-separated list of allowed frontend origins for CORS
# Include all dev ports you use (Vite, preview, local webserver, etc.)
CLIENT_ORIGIN="https://meghdoottextiles.com,https://www.meghdoottextiles.com,http://localhost:8080,http://localhost:8081,http://localhost:5173"

# API port (defaults to 5000)
PORT="5080"
